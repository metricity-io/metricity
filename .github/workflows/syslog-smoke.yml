name: Syslog Smoke

on:
  push:
    branches:
      - master

jobs:
  syslog-smoke:
    strategy:
      matrix:
        os: [macos-14, ubuntu-22.04, ubuntu-22.04-arm]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download Zig
        run: ./zig/download.sh

      - name: Check Zig version
        run: ./zig/zig version

      - name: Install netcat (Ubuntu)
        if: contains(matrix.os, 'ubuntu-')
        run: |
          sudo apt-get update
          sudo apt-get install -y netcat-openbsd

      - name: Build metricity
        run: |
          set -eo pipefail
          ./zig/zig build

      - name: Run syslog smoke test
        shell: bash
        env:
          LOG_PATH: ${{ runner.temp }}/metricity-syslog-smoke-${{ matrix.os }}.log
        run: |
          ./.github/ci/run-syslog-smoke.sh examples/basic.toml

      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: syslog-smoke-logs-${{ matrix.os }}
          path: ${{ runner.temp }}/metricity-syslog-smoke-${{ matrix.os }}.log
